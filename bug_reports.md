# Bug Report — EShop SUT

## 1. BẢNG TỔNG HỢP DANH MỤC LỖI

| Defect ID | Defect Title | Defect Description | Function ID | Severity | Reported By | Date Reported | Status | Comment |
| :---: | :--- | :--- | :---: | :---: | :--- | :---: | :---: | :--- |
| **B001** | [FR-05] Lỗ hổng SQL Injection trên endpoint `GET /api/products` khi tìm kiếm | **Pre-conditions:** Backend server EShop đang chạy ở `http://localhost:3000`.<br>**Steps to Reproduce:**<br>1. Gửi request `GET /api/products?search=' UNION SELECT id, name, email, password, 5, 6 FROM users--`.<br>2. Quan sát kết quả phản hồi.<br>**Actual Result:** Câu truy vấn SQL bị nối chuỗi trực tiếp (`WHERE name LIKE '%${searchQuery}%'`), dẫn đến câu lệnh SQL bị bẻ gãy và dump toàn bộ tài khoản, mật khẩu plaintext của Admin/Users.<br>**Expected Result:** Hệ thống phải sử dụng Parameterized Query để escape chuỗi an toàn theo yêu cầu **SEC-05**, chỉ tìm kiếm theo chuỗi nguyên bản.<br>**Environment:** Node.js Express, SQLite3, Windows 11. | FR-05 | **Critical** | NMQuang | 30-08-2026 | Open | Vi phạm nghiêm trọng yêu cầu bảo mật SEC-05. Đã bắt trúng qua TC_FR05_22, TC_FR05_23, TC_FR05_27. |
| **B002** | [FR-05] Rò rỉ thông tin cấu trúc CSDL và trả về sai Content-Type (HTML thay vì JSON) khi SQL lỗi | **Pre-conditions:** Server EShop đang chạy.<br>**Steps to Reproduce:**<br>1. Gửi request `GET /api/products?search=iPhone'`.<br>2. Kiểm tra Header `Content-Type` và Body phản hồi.<br>**Actual Result:** Trả về HTTP 500 với body là chuỗi HTML `<h1>Database Error</h1><p>SQLITE_ERROR: unrecognized token: "'%"</p>` và Header `Content-Type: text/html`.<br>**Expected Result:** API luôn phải trả về định dạng `application/json` chuẩn `{"error": "Internal server error"}` và không làm lộ chi tiết lỗi kỹ thuật của SQLite.<br>**Environment:** Node.js Express, SQLite3, Postman/Newman. | FR-05 | **High** | NMQuang | 30-08-2026 | Open | Vi phạm nguyên tắc bảo mật Information Disclosure và vi phạm API Contract Schema. |
| **B003** | [FR-08] Thiếu validation cho `total_amount` (chấp nhận tiền 0 đồng, số âm và số quá lớn) | **Pre-conditions:** User đã đăng nhập, có Bearer token hợp lệ.<br>**Steps to Reproduce:**<br>1. Gửi `POST /api/checkout` với body `{"total_amount": -100, "shipping_address": "123 Le Loi"}`.<br>2. Quan sát kết quả phản hồi.<br>**Actual Result:** Server chấp nhận và trả về HTTP 200 OK kèm `orderId`, lưu đơn hàng có giá trị âm vào CSDL.<br>**Expected Result:** Server phải validate `total_amount > 0` và trả về HTTP 400 Bad Request kèm thông báo lỗi hợp lệ.<br>**Environment:** Node.js Express, SQLite3, Postman/Newman. | FR-08 | **Critical** | NMQuang | 31-08-2026 | Open | Lỗ hổng tài chính nghiêm trọng cho phép đặt đơn hàng âm tiền để trục lợi. |
| **B004** | [FR-08] Thiếu validation cho `shipping_address` (chấp nhận chuỗi rỗng và khoảng trắng) | **Pre-conditions:** User đã đăng nhập, có Bearer token hợp lệ.<br>**Steps to Reproduce:**<br>1. Gửi `POST /api/checkout` với body `{"total_amount": 100, "shipping_address": "   "}`.<br>2. Quan sát kết quả phản hồi.<br>**Actual Result:** Server tạo đơn hàng thành công với HTTP 200 OK.<br>**Expected Result:** Bắt buộc `shipping_address` phải là chuỗi không rỗng có độ dài tối thiểu (minLength >= 5 ký tự) và trả về HTTP 400.<br>**Environment:** Node.js Express, SQLite3, Postman/Newman. | FR-08 | **High** | NMQuang | 31-08-2026 | Open | Gây ra dữ liệu rác trong hệ thống vận chuyển và xử lý đơn hàng. |
| **B005** | [FR-08] Lỗ hổng nghiệp vụ: Cho phép checkout khi giỏ hàng rỗng và cho phép Client tự sửa giá tiền (Price Tampering) | **Pre-conditions:** User đã đăng nhập.<br>**Steps to Reproduce:**<br>1. Không thêm hàng vào giỏ (giỏ hàng rỗng).<br>2. Gửi request `POST /api/checkout` với `{"total_amount": 1, "shipping_address": "HCM"}`.<br>**Actual Result:** Server tạo đơn hàng thành công mà không đối soát giỏ hàng `userCarts[userId]`.<br>**Expected Result:** Server phải kiểm tra giỏ hàng có hàng và tự tính toán lại tổng tiền từ giỏ hàng, từ chối tạo đơn nếu giỏ hàng rỗng hoặc số tiền không khớp.<br>**Environment:** Node.js Express, SQLite3, Postman/Newman. | FR-08 | **Critical** | NMQuang | 31-08-2026 | Open | Lỗ hổng nghiêm trọng phá vỡ hoàn toàn quy trình thương mại điện tử (Broken Business Logic). |
| **B006** | [FR-08] Server sập và trả về HTML khi nhận Content-Type không phải JSON | **Pre-conditions:** Server EShop đang chạy.<br>**Steps to Reproduce:**<br>1. Gửi request `POST /api/checkout` với Header `Content-Type: text/plain` và body là text thô.<br>**Actual Result:** Trả về HTTP 500 với body HTML thô.<br>**Expected Result:** Trả về HTTP 400 hoặc 415 Unsupported Media Type dạng JSON.<br>**Environment:** Node.js Express, Postman/Newman. | FR-08 | **Medium** | NMQuang | 31-08-2026 | Open | Vi phạm chuẩn API Error Handling và Content-Type contract. |
| **B007** | [FR-18] Lỗ hổng Broken Access Control (SEC-03): User thường có thể xem danh sách và cập nhật trạng thái đơn hàng của Admin | **Pre-conditions:** Server EShop đang chạy, User thường đăng nhập lấy Bearer token (`role: 'user'`).<br>**Steps to Reproduce:**<br>1. Gửi request `GET /api/admin/orders` với Header `Authorization: Bearer <userToken>`.<br>2. Gửi request `PUT /api/admin/orders/1/status` với Header `Authorization: Bearer <userToken>` và body `{"status": "confirmed"}`.<br>**Actual Result:** Cả 2 request đều thực thi thành công trả về HTTP 200 OK vì endpoint chỉ dùng middleware `authenticateToken` mà không kiểm tra `role === 'admin'`.<br>**Expected Result:** Server phải từ chối truy cập và trả về HTTP 403 Forbidden theo đúng yêu cầu bảo mật SEC-03 (Role-Based Access Control).<br>**Environment:** Node.js Express, SQLite3, Postman/Newman. | FR-18 | **Critical** | NMQuang | 01-09-2026 | Open | Lỗ hổng phân quyền nghiêm trọng vi phạm trực tiếp yêu cầu an ninh SEC-03 ([GitHub Issue #7](https://github.com/QuangNg27/SoftwareTesting_HW06/issues/7)). |
| **B008** | [FR-18] Lỗ hổng logic máy trạng thái: Cho phép đơn hàng đã hủy (canceled) chuyển thẳng sang đã giao hàng (delivered) | **Pre-conditions:** Server EShop đang chạy, Admin có Bearer token hợp lệ, đơn hàng có ID = 5 đang ở trạng thái `canceled`.<br>**Steps to Reproduce:**<br>1. Gửi request `PUT /api/admin/orders/5/status` với body `{"status": "delivered"}`.<br>**Actual Result:** Server cập nhật trạng thái thành công HTTP 200 OK do dòng mã lỗi: `if (currentStatus === "canceled" && status === "delivered") isValidTransition = true;`.<br>**Expected Result:** Đơn hàng đã hủy (Terminal/Dead State) không được phép chuyển thẳng sang đã giao hàng, phải trả về HTTP 400 Bad Request kèm thông báo lỗi chuyển trạng thái không hợp lệ.<br>**Environment:** Node.js Express, SQLite3, Postman/Newman. | FR-18 | **High** | NMQuang | 01-09-2026 | Open | Lỗ hổng logic máy trạng thái nghiệp vụ (Broken State Machine Invariant) ([GitHub Issue #8](https://github.com/QuangNg27/SoftwareTesting_HW06/issues/8)). |
| **B009** | [FR-18] Thiếu validation cho path parameter :id (không kiểm tra số nguyên dương hợp lệ) | **Pre-conditions:** Server EShop đang chạy, Admin có Bearer token hợp lệ.<br>**Steps to Reproduce:**<br>1. Gửi request `PUT /api/admin/orders/-1/status` hoặc `PUT /api/admin/orders/abc/status`.<br>**Actual Result:** Server vẫn thực hiện truy vấn SQL `SELECT status FROM orders WHERE id = -1` và trả về `404 Order not found` thay vì báo lỗi định dạng tham số `400 Bad Request`.<br>**Expected Result:** Server phải kiểm tra kiểu dữ liệu của path parameter `:id` là số nguyên dương $> 0$ trước khi truy vấn CSDL.<br>**Environment:** Node.js Express, SQLite3, Postman/Newman. | FR-18 | **Medium** | NMQuang | 01-09-2026 | Open | Thiếu kiểm soát dữ liệu đầu vào trên tầng định tuyến (Input Validation) ([GitHub Issue #9](https://github.com/QuangNg27/SoftwareTesting_HW06/issues/9)). |



